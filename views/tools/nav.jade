ul.nav.nav-pills
  each tool in tools
    li(class="#{tool.id==t.id?'active':''}")
      if tool.link
        a.title(href="#{tool.link}") #{tool.title}
      else
        a.title(href="/tools/#{tool.name}") #{tool.title}